### DROP

```sql
DROP DATABASE db_name; -- удалить БД с именем db_name
DROP TABLE table_name; -- удалить таблицу с именем table_name
DROP FROM table_name WHERE id = 7; -- удалить строки из таблицы table_name с id 7
DROP FROM table_name; -- удалить все строки из таблицы table_name
```